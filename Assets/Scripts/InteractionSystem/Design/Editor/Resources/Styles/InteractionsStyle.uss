
.interactionNodeView #title {
	padding-left: 18px;
	padding-right: 24px;
}

/* Temporarily disabled - since we're placing a port over it */
.interactionNodeView #title-button-container {
	display: none;
}

.interactionNodeView .portView.port.type-BlueGraphExamples-ExecGraph-ExecData {
	--port-color: #ffffff;
}

.interactionNodeView .portView.port.execOutPortView #type {
	visibility: hidden;
	width: 1px;
}

.interactionNodeView .portView.port.execInPortView #type {
	visibility: hidden;
	width: 1px;
}

.interactionNodeView .portView.port.execInPortView {

/* This is dumb and hacky but it lets us slap these ports into the 
	header without having to move around the DOM */
	
	position: absolute;
	top: -28px;
	left: 0;
}

.interactionNodeView .portView.port.execOutPortView {
	position: absolute;
	top: -28px;
	right: 0;
}

.interactionNodeView #output {
	min-width: 30px;
}


.interactionNodeView #input {
	min-width: 30px;
}

.interactionNodeView.port.type-BlueGraphExamples-ExecGraph-ExecData #connector {
	width: 10px;
	height: 10px;
	border-top-left-radius: 0;
	border-bottom-left-radius: 0;
	border-top-right-radius: 5px;
	border-bottom-right-radius: 5px;
}

.interactionNodeView.port.type-BlueGraphExamples-ExecGraph-ExecData #connector > #cap {
	border-top-left-radius: 0;
	border-bottom-left-radius: 0;
	border-top-right-radius: 5px;
	border-bottom-right-radius: 5px;
}